# Workspace root for the paper_scanner monorepo.
# This package is never published; it only hosts melos tooling.
name: paper_scanner_workspace
description: Development workspace for the paper_scanner federated plugin.
publish_to: none
version: 0.0.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dev_dependencies:
  melos: ^6.3.2
